{% extends 'base.html.twig' %}

{% block title %}Hôtelhouse
{% endblock %}

{% block body %}


	<div id="carouselExampleCaptions" class="carousel slide carousel-fade" data-bs-ride="carousel">
		<div class="carousel-indicators">
			<button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
			<button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="1" aria-label="Slide 2"></button>
			<button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="2" aria-label="Slide 3"></button>
		</div>
		<div class="carousel-inner" data-bs-interval='3500'>
			{% for slider in sliders %}
				{% if slider.chambre == null %}
					<div class="carousel-item {% if slider.ordre == 1 %} active {% endif %}">
						<img src="{{slider.photo}}" class="d-block w-100" alt="" style=" object-fit: cover;">
						<div class="carousel-caption d-none d-md-block">
							<a href="{{path('chambres')}}" class="btn btn-info">Hôtelhouse | découvrez nos chambre</a>
						</div>
					</div>
				{% endif %}
			{% endfor %}
		</div>
		<button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
			<span class="carousel-control-prev-icon" aria-hidden="true"></span>
			<span class="visually-hidden">Précédent</span>
		</button>
		<button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
			<span class="carousel-control-next-icon" aria-hidden="true"></span>
			<span class="visually-hidden">Suivant</span>
		</button>
	</div>
	<div class=" lg-col-8 pt-4 bg-white mb-8 ">
		<h1 class="font-serif mb-3">Hôtelhouse</h1>
		<p class="text-lg">Il était une fois le palace de demain. Fidèle à sa ville extraordinaire, l’Hôtelhouse n’est pas un palace ordinaire. Ici, sur la prestigieuse avenue Montaigne, adresse de la Haute Couture française, notre hôtel vous offre avec fierté le meilleur de Paris.</p>
	</div>
	<div class="d-flex flex-col md:flex-row l items-center justify-content-around mb-4 ">
		<div class="text-sm d-flex flex-row items-center w-full">
			<span aria-hidden="true" class="fas fa-location " title="Location" alt="">
				<i class="fas fa-hotel"></i>
			</span>
			<span class="sr-only">Lieu:</span>
			<div>
				Hôtelhouse<br>
				1 avenue Montaigne, 75008 Paris</div>
		</div>
		<div class="text-sm d-flex flex-row items-center w-full">
			<i class="fas fa-phone"></i>
			<span aria-hidden="true" class="dc-icon dc-call " title="Call" alt=""></span>
			<span class="sr-only">Téléphone:</span>
			<div>
				+33 (0) 1 23 45 67 89</div>
		</div>
	</div>
{% endblock %}
